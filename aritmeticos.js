const [a, b, c , d] = [3, 5, 1, 15]
//utilizando destructure

const soma = a + b + c + d
const subratacao = d - b
const multiplicacao = a * b
const divisao = d / a
const modulo = a % 2

console.log(soma, subratacao, multiplicacao, divisao, modulo)